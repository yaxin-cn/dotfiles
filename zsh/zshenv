# Use vim as the visual editor
export VISUAL=vim
export EDITOR=$VISUAL

# nvm mirror
export NVM_NODEJS_ORG_MIRROR=https://npm.taobao.org/dist
# reset PATH
PATH=/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin:/usr/bin/X11:/usr/games
# add composer path
PATH=$HOME/.composer/vendor/bin:$PATH
# add custom bin path
PATH=$HOME/app/bin:$PATH
# add global nodejs
test -d /usr/local/node && PATH=/usr/local/node/bin:$PATH
# add nvm and local node path
if which nvm &> /dev/null;then
    installed_latest=`nvm_ls | tail -1`
    PATH=${NVM_DIR}/versions/node/${installed_latest}/bin:$PATH
fi
export PATH

# Local config
[[ -f ~/.zshenv.local ]] && source ~/.zshenv.local
